import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mx.com.shark.technologies.education',
  appName: 'educacion',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
